# Implementation Plan

1. Add title/highlight normalization helpers and unit tests.
2. Add the two cover presets and detached DOM renderer.
3. Bundle Noto Sans SC Bold and LXGW WenKai Lite with license files and `@font-face` declarations.
4. Add PNG capture and overflow/font-loading tests using focused mocks.
5. Run Web tests/lint/build before handing off to the delivery task.
